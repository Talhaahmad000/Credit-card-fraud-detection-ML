# Credit-card-fraud-detection-ML
This project uses Machine Learning to detect fraudulent credit card transactions. It analyzes transaction data and builds a model to classify transactions as fraud or legitimate. Data preprocessing, feature scaling, and model training are performed using Python and Scikit-learn. The model is evaluated using accuracy
